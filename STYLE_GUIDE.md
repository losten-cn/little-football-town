# 《足球小镇》终极视觉设计论稿（V1.0 终稿）

> —— 一份可直接交付程序与美术的"像素宪法"
>
> **状态**: 已锁定 (Locked)
> **日期**: 2026-07-10
> **版本**: V1.0 终稿
> **地位**: 项目根目录唯一视觉权威文档。所有 art-bible / HUD 设计 / UX 视觉规格 中与本稿冲突的条款，以本稿为准。

---

## 第一部分：核心基石（不可动摇的物理法则）

| 法则 | 值 | 备注 |
|------|-----|------|
| **引擎坐标** | 2D 俯视（Top-down），非等距 | 建筑正面必须朝向屏幕下方（玩家） |
| **Tile 尺寸** | 32×32 像素（原始资产尺寸） | — |
| **显示缩放** | 2倍整数缩放（运行时每个 Tile 显示为 64×64 像素） | 禁止使用 1.5x、1.8x 等非整数缩放，否则 Zpix 字体与像素边缘将产生不可逆的模糊 |
| **适配分辨率** | 原生锁定 1920×1080，向下兼容 1280×720 | 缩至 1.5x 时允许轻微模糊，但 UI 必须保持整数锚点 |
| **视野窗口（Viewport）** | 30 列 × 17 行（即 960×544 原始像素，放大 2 倍后为 1920×1088 像素） | 纵向略超，配合 HUD 完美裁切 |
| **绘制排序（Z-Order）** | 严格基于 Y轴（世界坐标）排序 | 角色脚底 Y 值越大，绘制层级越靠上（遮挡越前）。这是实现"建筑正面遮挡角色"的唯一正确算法，严禁使用图层硬分区 |

---

## 第二部分：世界架构（画布与边界）

### 有效显示区域

| 区域 | 坐标 | 高度 | 内容 |
|------|------|------|------|
| 顶部 HUD | Y=0~72 | 72px | 赛季/日期/经费/AP/倒计时/菜单 |
| 底部导航 | Y=1016~1080 | 64px | 球员入口 / 比赛入口 / 队徽 |
| 中央世界 | Y=73~1015 | 944px (29.5 Tile) | 小镇地图主体 |
| 左右安全边距 | X=0~32 / X=1888~1920 | 各 32px (1 Tile) | 黑色或深棕安全边距（模拟老电视过扫区，增强复古感） |

### 世界尺寸规划

建议地图总尺寸为 **60 列 × 34 行**（视角窗口刚好滚动 2 倍面积），确保俱乐部会所、训练场、医疗室、青训营、球场入口在缩放时永不超出边界。

---

## 第三部分：色彩与光（情绪系统）

### 绝对色板（7色闭环）

| 颜色 | 色号 | RGB | 角色 |
|------|------|-----|------|
| 奶油色 | `#F2E8D5` | (242,232,213) | 基础亮色、面板、留白 |
| 小镇金 | `#D6B35A` | (214,179,90) | 温暖、自豪、进展、强调 |
| 俱乐部红 | `#B84A4A` | (184,74,74) | 团队精神、比赛能量、警报 |
| 冷静蓝 | `#5E7FA3` | (94,127,163) | 信息、日程、次级结构 |
| 球场绿 | `#6F8F5B` | (111,143,91) | 成长、健康、训练、自然 |
| 大地棕 | `#8A6B4F` | (138,107,79) | 建筑、路径、木材、日常 |
| 石板灰 | `#4C4A4A` | (76,74,74) | 文字、轮廓、阴影 |

### 季节调色偏移（强制全局滤镜）

| 季节 | 规则 |
|------|------|
| **春** | 地面基调提亮 +10% 黄，植被新增浅绿 `#8FBC6A` 点缀 |
| **夏** | 饱和度微增 +5%，草皮呈现饱满深绿 `#5A7A4A` |
| **秋** | 色温偏暖，草地混入 `#C58A3A`（大地棕变体）和枯黄 `#BFA26A` |
| **冬** | 整体明度拉高，地面覆盖 30% 透明白色雪层，天空光模拟冷白，但建筑窗户保留暖黄灯光 `#F2E8D5` |

### 时间冷暖（仅视觉提示，无阴影系统）

| 场景 | 规则 |
|------|------|
| **日常经营** | 全局暖白光照（无阴影或极小 2px 投影） |
| **比赛黄昏** | 全局叠加橙色高光层，对比度拉高，泛光灯柱产生向下的 4px 硬投影 |
| **夺冠夜晚** | 背景星空粒子，建筑轮廓保留冷蓝 `#5E7FA3` 背光，烟花炸点为俱乐部红+金 |

---

## 第四部分：界面交互（HUD/UI 铁律）

### 顶部状态栏（Y=0~72，高72px）

- 底色 `#FFF2D2`，底部压 2px `#C58A3A` 木质边框
- **左区**：赛季/日期（Zpix 16px，`#3A2A1A`）+ 天气图标（24×24）
- **中区**：经费（💰 + 数字 `#D6B35A`）、运动点数（⚡ + 数字 `#5E7FA3`）、行动窗口（窄标签 72×28，`#6F8F5B` 背景，白字）
- **右区**：比赛倒计时（`#B84A4A`）+ 汉堡菜单（32×32）

### 底部导航栏（Y=1016~1080，高64px）

- 底色 `#8A6B4F` + 奶油细条纹，顶部压 2px `#C58A3A` 边框
- 仅两个按钮：左侧「👥 球员入口」，右侧「⚽ 比赛入口」，中间固定 32×32 队徽
- 悬停填充 `#FFF2D2` + 金边，激活文字变 `#B84A4A` + 左侧 4px 红块指示器

### 通用面板（弹窗）

- 边框 2px `#3A2A1A` + 内框 1px `#C58A3A`，背景 `#FFF2D2`，标题栏 `#D6B35A`
- 圆角 = **0**（绝对直角）
- 关闭按钮 16×16，悬停变红
- 尺寸三档：小 640×480，中 800×560，大 960×640

### Zpix 字体排版（唯一字体）

- 关闭抗锯齿，强制 4px 网格对齐
- 最大字号 24px（仅比赛横幅），常规 16px/18px/20px
- 禁用状态：原色 + 50% 白色蒙层

### 比赛直播 UI（唯一例外暗色）

- 顶部栏切换为 `#2A1F1A`（深木炭），文字反白
- 比分牌：`#B84A4A` 底板 + `#FFF2D5` 大字，置顶中央
- 计时器：冷静蓝 `#5E7FA3`
- 终场哨响后 5 秒渐变动画恢复暖色

---

## 第五部分：实体与空间（内容资产规范）

### 建筑（2×2 Tile 起，64×64 像素基底）

- 必须露出斜屋顶（占 1/3）和正面门廊/遮阳篷（占 2/3）
- **升级表现**：草皮变整齐（边缘锐利）、墙面增加粉刷线条、屋顶瓦片颜色加深、门前增加盆栽/长椅
- **道具密度**：关键建筑（会所/训练场）周围聚集丰富细节簇（长椅、自行车、晾衣绳、海报）；路径和过渡区域保持稀疏，确保加载性能

### 角色（32×48 像素，含头顶留白）

- **头身比**：头占 12px，身体占 24px，腿占 12px（运动比例）
- **三要素**：1 个剪影特征（如后卫宽肩/前锋微前倾）+ 1 个颜色强调（鞋带/发带/队长袖标的队色）+ 1 个个人配件（护腿板/战术板/口哨/围裙）
- **动画**：待机（呼吸微动 2帧）、走路（4帧循环）、跑动（6帧快循环）。比赛时自动跑动为 8 方向简化（上下左右+斜向）

### 球场（占 14 行 × 20 列 Tile）

- 坐落于小镇中央偏下，四周保留 2-3 Tile 边距供观众、围栏、横幅使用
- 草皮纹理包含磨损斑秃（深绿与浅绿交替），白线为 2px 粗
- **社区属性**：一侧有木质小看台（100×64 像素），看台上分布像素居民（随机挥舞围巾）。场边有手绘横幅（文字可用 Zpix 16px 写"FORZA TOWN"）

---

## 第六部分：体验红线（绝对禁止清单）

1. ❌ 禁止等距菱形网格（Z轴排序将崩溃）
2. ❌ 禁止霓虹/赛博/冷科幻/夜店色（违背温暖社区调性）
3. ❌ 禁止全局暗色 UI（仅比赛直播 5 分钟内可用）
4. ❌ 禁止比赛纯文本结果（必须播放自动跑动动画）
5. ❌ 禁止超大头身 Q 版（严禁 1:1 头身）
6. ❌ 禁止开罗建筑剪影/角色比例复制（借鉴节奏，不借鉴造型）
7. ❌ 禁止矢量/光滑材质混入（模糊像素边界）
8. ❌ 禁止小镇呈现污垢/破败/绝望感（挫折也要向前，色调保持整洁）
9. ❌ 禁止 HUD 遮挡世界中央 60%×50% 区域（信息永远悬浮，不叠加覆盖）
10. ❌ 禁止把足球奇观（泛光灯/大赛氛围）用于日常经营，仅保留给特殊峰值事件

---

## 第七部分：启动行动清单（美术生产第一刀）

按此顺序产出最小可行资产（Vertical Slice）：

1. **基础 TileSet**：草地（春/夏/秋/冬 4 版）、泥土路、木板路、白色围栏
2. **核心建筑**：俱乐部会所（4×4 Tile）+ 训练场（3×3 Tile）+ 社区球场（14×20 Tile 白线底纹）
3. **主角角色**：1 名前锋球员（32×48，含跑动 6 帧）+ 1 名NPC教练（持战术板待机 2 帧）
4. **顶部 HUD**：完整的 1920×72 静态图（所有图标占位）
5. **比赛测试场景**：将球员置于球场中央，测试 Y 轴排序是否遮挡正确

---

## 终极定稿宣言

以上每一像素均服务于《足球小镇》的"社区长出来的足球"这一核心叙事。美术资产脱离此规范即为无效资产，程序渲染脱离此逻辑即为技术债务。请将此文档作为项目根目录下的 `STYLE_GUIDE.md` 封存，每日开工前阅读一遍「绝对禁止清单」。

---

## 附录：与现有文档的关系

| 现有文档 | 关系 | 处理 |
|---------|------|------|
| `design/art/art-bible.md` | 部分冲突（色板、暗色UI许可） | 以本稿为准；art-bible 保留作为设计演进参考 |
| `design/art/hud-visual-design.md` | 色板冲突（`#1A1A2E` vs `#F2E8D5`） | 以本稿色板为准；HUD layout 框架仍有效 |
| `design/ux/hud.md` | Layout 兼容 | 本稿精确定义了 HUD 像素坐标，补充 UX spec |
| `design/ux/town-main-view.md` | Zone B 定义兼容 | 本稿将 Zone B 具象化为 30×17 tile 世界画布 |
| `production/session-state/visual-direction-recalibration-2026-07-09.md` | P0 问题已解决 | 像素精度、视角、色板、Viewport 已在本稿锁定 |
